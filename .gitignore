# Environment
.env

# Writable directory (logs, cache, sessions, debugbar)
writable/logs/
writable/cache/
writable/session/
writable/debugbar/
writable/uploads/
writable/backups/

# Database SQLite — file ini berisi data live.
# Sengaja di-track di git agar data awal tersedia saat deploy pertama.
# SEBELUM deploy ulang, jalankan: bash scripts/backup-db.sh
# writable/assets.sqlite

# Vendor
vendor/

# OS
.DS_Store
Thumbs.db
