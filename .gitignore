# Environment
.env

# Writable directory (logs, cache, sessions, debugbar)
writable/logs/
writable/cache/
writable/session/
writable/debugbar/
writable/uploads/
writable/backups/

# Database SQLite — file live tidak di-commit ke Git.
# Gunakan scripts/backup-db.sh dan scripts/restore-db.sh untuk pengelolaan backup.
# Seed JSON (employee-seed.json, endpoint-seed.json, asset-dashboard-data.json) tetap di Git.
writable/assets.sqlite
writable/assets.sqlite-shm
writable/assets.sqlite-wal

# Backup database
backups/

# Vendor
vendor/

# OS
.DS_Store
Thumbs.db
